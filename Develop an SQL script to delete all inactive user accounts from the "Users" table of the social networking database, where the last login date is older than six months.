CREATE TABLE Users (
    UserID INT PRIMARY KEY,
    Username VARCHAR(50) NOT NULL UNIQUE,
    Email VARCHAR(100) NOT NULL UNIQUE,
    LastLoginDate DATE,
    IsActive BOOLEAN NOT NULL
);

DELETE FROM Users
WHERE IsActive = FALSE AND LastLoginDate < DATE_SUB(CURDATE(), INTERVAL 6 MONTH);
