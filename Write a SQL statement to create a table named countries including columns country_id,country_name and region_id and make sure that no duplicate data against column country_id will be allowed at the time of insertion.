CREATE DATABASE my_database;
USE my_database;
CREATE TABLE countries (
    country_id INT PRIMARY KEY,  -- Primary Key ensures no duplicates
    country_name VARCHAR(255) NOT NULL,
    region_id INT
);
